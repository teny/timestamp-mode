Emacs Minor-mode that inserts time stamp for shell-mode and ssh-mode.
(based comint-mode)

Usage:

Please write the following codes in your ~/.emacs.

(set 'timestamp-mode-enable t)
(require 'timestamp-mode)
